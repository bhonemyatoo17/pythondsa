nums = [3,4,7,2,4] #a list array
for num in nums:
    if num%2:
        print(f"{num} is Odd")
    else:
        print(f"{num} is Even")
#list functions
# nums.append(10)      # adds 10 to the end
# nums.remove(4)       # removes the first occurrence of 4
# nums.pop()           # removes the last item
# nums.pop(0)          # removes item at index 0
# len(nums)            # length of the list
# nums.sort()          # sorts the list